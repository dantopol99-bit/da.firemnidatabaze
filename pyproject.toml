[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "firemni-databaze"
version = "0.1.0"
description = "Datový produkt z veřejných registrů ČR"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
